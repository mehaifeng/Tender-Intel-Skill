interface:
  display_name: "招标情报"
  short_description: "检索最近72小时、匹配医院并推送IVD招标采购情报"
  default_prompt: "Use $tender-intel to 执行最近72小时IVD招标情报的完整流程，匹配医院信息并按固定13字段推送。"
policy:
  allow_implicit_invocation: true
