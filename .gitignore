# 含凭据，禁止提交
# config/doubao.json 是已删除的豆包适配器遗留文件（2026-09-02），规则保留以防本地残留被误提交
config/doubao.json
config/webhook.json

# 可分发包内含本地凭据，只保留在本机，不提交版本库
dist/

# 本机权限允许清单，因机器而异
.claude/settings.local.json

# 调试探针与临时产物（含 token 明文、下载的附件）
.tmp/
__pycache__/
*.pyc

# 去重表：仓库里只保留空骨架，运行后累积的业务情报不再提交
# 注意：该文件已被跟踪，.gitignore 对它无效，实际靠 git update-index --skip-worktree 生效
# 恢复跟踪：git update-index --no-skip-worktree data/seen.json
data/seen.json

# macOS 目录元数据
.DS_Store

# 医院索引的中间产物：未压缩版与重建前备份，只留 hospitals.min.json.gz 入库
data/hospitals.min.json
data/*.bak
